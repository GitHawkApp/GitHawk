query RepositoryInfo($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    id
    defaultBranchRef {
      name
    }
    hasIssuesEnabled
    watchers {
      totalCount
    }
    stargazers {
      totalCount
    }
    forkCount
  }
}
